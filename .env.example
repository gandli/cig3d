# Environment Variables

Copy this file to `.env` and fill in your values.

```env
# No environment variables required for development
# The project is fully static, all assets are self-contained
```
